from pathlib import Path

debugging = True
building_alpha = 0.3
base_aircraft_model_path    = Path('.') / 'data' / 'models'
datalogger_save_path = Path('.') / 'data' / 'dataloggers'
cfd_data_folder_path = Path('.') / 'data' / 'wind_fields'
sweeps_path = Path('.') / 'data' / 'sweeps'